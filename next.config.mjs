/** @type {import('next').NextConfig} */
const nextConfig = {
    images : {
        remotePatterns : [
            {
                hostname : "oceanic-hamster-313.convex.cloud"
            }
        ]
    }
};

export default nextConfig;
